document.addEventListener("DOMContentLoaded", () => {
    const button = document.getElementById("alertBtn");
    button.addEventListener("click", () => {
        alert("Hello! You clicked the premium button successfully.");
    });
});
